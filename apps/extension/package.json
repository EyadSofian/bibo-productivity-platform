{
  "name": "@ctracking/extension",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "employeetrack browser extension (Manifest V3) — reports active tab to the local app.",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.1.11"
  }
}
